import 'package:flutter/material.dart';

class OldChatWidget extends StatelessWidget {
  const OldChatWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
